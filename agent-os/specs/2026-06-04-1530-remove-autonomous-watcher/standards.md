# Standards

**N/A** — matching the precedent set in `2026-05-16-1854-rapture-mac-v1-local-capture/`. The repo's `agent-os/standards/` entries are placeholders pending `/index-standards`, and the available categories (api, migrations, models, frontend) don't apply to a Swift menu-bar app + repo cleanup.
